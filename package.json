{
  "name": "my-tutor",
  "version": "1.0.0",
  "description": "Personal tutor app",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install --production && node server.js"
  }
}
